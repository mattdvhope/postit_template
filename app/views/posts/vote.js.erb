<% if @vote.valid? %>
  $('#post_<%= @post.slug %>_votes').html('<%= @post.total_votes %> votes');
<% else %>
  // alert("You can only vote on '<%= "#{@post.title}" %>' once.");

  $('#post_vote_error_<%= @post.to_param %>').show();

<% end %>