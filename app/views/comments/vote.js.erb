<% if @vote.valid? %>
  $("#comment_<%= @comment.id %>_votes").html("<%= @comment.total_votes %> votes");
<% else %>
  // alert("You can only vote on a comment once.");

  $('#comment_vote_error_<%= @comment.to_param %>').show();

<% end %>